spring:
  main:
    banner-mode: "off"

robin:
  port: 5010
  binlog:
    file: 'G:\Google Drive\workspace\binlog\data.data'
#   如果是master则配置master数据
  master:
    backup:
      port: 5011
      ip: '127.0.0.1'
      interval: 1
      name: 'slave1'
